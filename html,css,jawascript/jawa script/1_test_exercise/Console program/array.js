//#region array 
// var color = ['red', 'green', 'blue'];
// color.forEach(printcolor); //print all elements in array through function
// color.pop(); //to remove last position data
// color.push(1); //to insert data in last position (this array can store all type of data?)
// console.log("--------------");
// color.forEach(printcolor);

// //function to print all elements in array
// function printcolor(color)
// {
//   console.log(color);
// }
//#endregion

//#region object data type array
var student = [
    {name : 'hari', roll_no : 10, cgpa : 7.5},
    {name : 'haran', roll_no : 01, cgpa : 5.7}
]
console.log(student);
console.log(student[0].cgpa);
//#endregion